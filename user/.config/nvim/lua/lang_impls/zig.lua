return {
    system_deps = {'zig'},
    -- tools = {},
    -- lazy = {},
    -- treesitter = {},
    -- null = function(null_ls) end,
    -- lsp = function(lspconfig, capabilities) end,
    -- dap = function() end,
}
